import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFFCC0000);
  static const onPrimary = Colors.white;
  static const error = Color(0xFFB00020);

  // Light theme
  static const background = Color(0xFFF5F5F5);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6B6B);
  static const divider = Color(0xFFE0E0E0);
  static const inputFill = Color(0xFFF9F9F9);
  static const muted = Color(0xFFF0F0F0);

  // Dark theme
  static const darkBackground = Color(0xFF0F0F0F);
  static const darkSurface = Color(0xFF1C1C1E);
  static const darkInput = Color(0xFF2C2C2E);
  static const darkBorder = Color(0xFF38383A);
  static const darkTextPrimary = Color(0xFFF2F2F7);
  static const darkTextSecondary = Color(0xFF8E8E93);
  static const darkMuted = Color(0xFF2C2C2E);
}
